# PRODUCT-STOCK-CRUD-MANAGER
This  Python project is a **Product Stock CRUD Manager** that uses **Create, Read, Update, Delete (CRUD)** operations to manage inventory. It handles core functions like adding new products, viewing stock levels, updating quantities, and deleting discontinued items, often leveraging a file or database for **data persistence**.
